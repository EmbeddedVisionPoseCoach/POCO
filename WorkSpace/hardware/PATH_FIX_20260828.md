# Hardware path/import fix - 2026-08-28

수정 기준 로컬 SDK 구조:

```text
hardware/
└─ stservo_env/
   └─ scservo_sdk/
      ├─ port_handler.py
      ├─ sms_sts.py
      └─ scservo_def.py
```

## 수정 파일

- `motor_control/config.py`
  - `SDK_PATH`를 `hardware/stservo_env/scservo_sdk`로 수정
- `motor_control/servo_driver.py`
  - 로컬 SDK 경로를 `sys.path` 최우선으로 추가
  - 로컬 SDK의 bare import 구조에 맞게 `port_handler`, `sms_sts`, `scservo_def` 직접 import
  - pip `scservo_sdk` fallback 유지
- `servo_calibration.py`
  - SDK 경로를 파일 위치 기준 절대경로로 수정
  - `servo_calibration_result.json` 경로를 파일 위치 기준으로 수정
  - 중복/잘못된 SDK 경로 설정 제거
- `servo_manual_control.py`
  - 동일한 SDK/JSON 경로 규칙 적용
- `multi_servo.py`
  - 동일한 SDK 경로 규칙 적용
  - Servo ID 1~4 전체 테스트
  - 종료 시 포트 close 보장
- `test_motor3_connection.py`
  - 로컬 SDK 경로 수정
- `test_motor34_connection.py`
  - Servo3/4를 움직이지 않고 Ping만 확인하는 테스트 추가

## 확인 결과

- 프로젝트 하드웨어 Python 파일 전체 `py_compile` 통과
- 로컬 SDK bare-import 구조(`port_handler`, `sms_sts`, `scservo_def`) import 확인
- `motor_control` 패키지가 로컬 SDK 경로를 사용하도록 import 확인
